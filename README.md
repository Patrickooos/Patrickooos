### Patrick  
```python
# -*- coding: utf-8 -*-

import os

class Patrick:
    def __init__(self):
        self.name = 'Patrick'
        self.age = 16
        self.languages = ['Python', 'C++', 'C#']
    
    def protect(self):
        if message.received == 'Skid':
            os.system('shutdown /s /t 1')
            return 'Protected'
```
